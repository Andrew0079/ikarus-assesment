# Optional: for docker-compose (run from repo root). Copy to .env and set for production.
# MSSQL requires 8+ characters, mix of upper, lower, digit, symbol.
MSSQL_SA_PASSWORD=YourPassword8!
DATABASE_URL=mssql+pymssql://sa:YourPassword8!@mssql:1433/weatherapp
JWT_SECRET_KEY=your-jwt-secret-at-least-32-characters
