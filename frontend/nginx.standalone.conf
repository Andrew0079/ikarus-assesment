# Standalone frontend (e.g. Railway): no /api proxy; set VITE_API_URL at build time to your backend URL.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
